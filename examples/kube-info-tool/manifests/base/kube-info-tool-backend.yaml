apiVersion: v1
kind: ServiceAccount
metadata:
  name: kube-info-tool
  namespace: mcp-test
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kube-info-tool-reader
rules:
  - apiGroups: [""]
    resources: ["namespaces", "pods"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kube-info-tool-reader
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kube-info-tool-reader
subjects:
  - kind: ServiceAccount
    name: kube-info-tool
    namespace: mcp-test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-info-tool
  namespace: mcp-test
  labels:
    app.kubernetes.io/name: kube-info-tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-info-tool
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kube-info-tool
    spec:
      serviceAccountName: kube-info-tool
      containers:
        - name: kube-info-tool
          image: ghcr.io/atippey/kube-info-tool:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: kube-info-tool-svc
  namespace: mcp-test
  labels:
    app.kubernetes.io/name: kube-info-tool
spec:
  selector:
    app.kubernetes.io/name: kube-info-tool
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
